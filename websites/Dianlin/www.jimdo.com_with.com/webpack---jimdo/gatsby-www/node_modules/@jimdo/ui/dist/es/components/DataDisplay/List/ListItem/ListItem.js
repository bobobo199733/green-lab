import { Icon } from '../../Icon/Icon.js';
import { useStyles } from '../../../../hooks/useStyles/useStyles.js';
import classNames from 'classnames';
import React from 'react';
import styles from './ListItem.module.scss.js';

var ListItem = function (props) {
  var _a;

  var marginBottomClass = useStyles({
    marginBottom: props.marginBottom || 'xs'
  });
  var classes = classNames(styles.listItem, marginBottomClass, (_a = {}, _a[styles.listItemCircle] = props.icon === undefined, _a), props.className);
  return React.createElement("li", {
    className: classes
  }, props.icon && React.createElement(Icon, {
    className: styles.listItemIcon,
    icon: props.icon
  }), React.createElement("span", {
    className: styles.listItemText
  }, props.text));
};

ListItem.displayName = 'ListItem';
export { ListItem };