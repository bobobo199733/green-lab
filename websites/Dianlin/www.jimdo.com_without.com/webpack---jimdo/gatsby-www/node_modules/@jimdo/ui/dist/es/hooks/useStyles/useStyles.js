import { getNonResponsiveClasses } from '../../utils/getNonResponsiveClasses/getNonResponsiveClasses.js';
import { getResponsiveClasses } from '../../utils/getResponsiveClasses/getResponsiveClasses.js';
import classNames from 'classnames';
import styles from './useStyles.module.scss.js';

function useStyles(props) {
  var classes = [];

  for (var _i = 1; _i < arguments.length; _i++) {
    classes[_i - 1] = arguments[_i];
  }

  var stylesResponsive = {
    margin: props.margin,
    'margin-top': props.marginTop,
    'margin-right': props.marginRight,
    'margin-bottom': props.marginBottom,
    'margin-left': props.marginLeft,
    padding: props.padding,
    'padding-top': props.paddingTop,
    'padding-right': props.paddingRight,
    'padding-bottom': props.paddingBottom,
    'padding-left': props.paddingLeft
  };
  var stylesNormal = {
    color: props.color
  };
  return classNames(classes, getResponsiveClasses(stylesResponsive, styles), getNonResponsiveClasses(stylesNormal, styles));
}

export { useStyles };