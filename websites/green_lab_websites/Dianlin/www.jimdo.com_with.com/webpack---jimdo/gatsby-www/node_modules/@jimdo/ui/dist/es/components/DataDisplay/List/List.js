import { useStyles } from '../../../hooks/useStyles/useStyles.js';
import classNames from 'classnames';
import React from 'react';
import styles from './List.module.scss.js';

var List = function (props) {
  var _a;

  var marginBottomClass = useStyles({
    marginBottom: props.marginBottom || 'l'
  });
  var noGutterClass = useStyles({
    padding: 'zero'
  });
  var classes = classNames(marginBottomClass, props.noGutter && noGutterClass, props.className, (_a = {}, _a[styles.small] = props.small, _a));
  return React.createElement("ul", {
    className: classes
  }, props.children);
};

List.displayName = 'List';
export { List };