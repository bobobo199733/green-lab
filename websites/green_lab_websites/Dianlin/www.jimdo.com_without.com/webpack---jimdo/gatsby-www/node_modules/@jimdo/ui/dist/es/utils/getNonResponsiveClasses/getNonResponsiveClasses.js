/**
 * retrieves a list of non-responsive classes
 * @param styles            key-value pairs of style definitions
 * @param stylesScssModule  reference to the actual scss module used to retrieve the styles
 */
var getNonResponsiveClasses = function (styles, stylesScssModule) {
  return Object.keys(styles).map(function (styleName) {
    var regularStyle = styles[styleName];

    if (regularStyle === undefined) {
      return null;
    }

    if (Array.isArray(regularStyle)) {
      return regularStyle.map(function (key, index) {
        return stylesScssModule["".concat(styleName, "-").concat(index, "-").concat(regularStyle[index])];
      });
    }

    return stylesScssModule["".concat(styleName, "-").concat(regularStyle)];
  });
};

export { getNonResponsiveClasses };